# fantasy-league

MVP fantasy football — Spring Boot (Java 21) + PostgreSQL + Redis + React (frontend planned)

## How to run (dev)
1. Configure DB in application.yml or use docker-compose: 

> docker-compose up –build 

2. Open http://localhost:8080

## Tech stack
- Java 21, Spring Boot 3
- PostgreSQL, Redis
- Docker, docker-compose